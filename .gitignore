# === Файлы, которые не публикуются в репозиторий ===

# Секреты и локальная конфигурация
.env
.env.local
.env.*
!.env.example

# Зависимости Node.js
node_modules/

# Логи и временные данные
logs/
*.log

# Кэш библиотеки phpqrcode (генерируется автоматически при первом использовании)
phpqrcode/cache/

# Отладочные скрипты, содержащие реальные учётные данные (не публикуются)
capture.js
to_nalog

# Сборочные артефакты в корне (актуальные релизы лежат в releases/)
mytax.ocmod.zip

# Системные файлы
.DS_Store
Thumbs.db
desktop.ini